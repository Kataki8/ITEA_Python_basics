импорт модуя random
переменная а = 0
переменная grail - случайное число от 1 до 400
пока переменная а не равна переменной grail 
  a задаётся пользователем
  если а меньше grail:
   напечатать "маленькое число"
  если а больше grail:
   напечатать "большое число
  иначе
   напечатать "угадал"