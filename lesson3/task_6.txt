импорт модуля регулярных выражений 
1. функция получить ввод пользователя
переменная h вводится пользователем
если h состоит из цифр
    возвращать значение h(целое число)
иначе: 
    возвращать none
2. функция проверяющая является ли ввод пользователя номером
телефона
если телефонная строка состоит из чисел или срез
телефонной строки это + и срез строки состоит из чисел
    возвращать правду
иначе
    возвращать ложь
3. функция получить номер тел (строка с вопросом: "Телефон: ")
h - вводится пользователем (напечать строку с вопросом)
если *запуск функции №2* правда:
    возвращать h
иначе: 
    возвращать none
4. функция добавить контакт (аргументы: имя контакта, номер
контакта, тел книга)
присваивание имени контакта к номеру
возвращать значение по имени контакта (по ключу)
5. функция получить номер контакта (аргументы: имя контакта,
словарь)
 возвращать значение из словаря по ключу (имени контакта)
6. функция получить имя контакта (арг: телефон контакта, 
тел книга)
для ключей и значений вернуть ключи значения
    если значение является номером телефон
        вернуть ключ
    вернуть none
7. функция удалить контакт (арг: имя контакта, тел книга)
если 



8. функция проверить номера (арг: тел книга)
повторяющиеся номера = 0
для ключей и значений вернуть пары (ключи и значения)
  строка проверяющая три одинаковые цифры в значении (номере)
     если h
       повторяющиеся номера = повторяющиеся номера + 1
возвращать повторяющиеся номера (переменная)
9. функция получить ввод
возвращать функцию получить номер с сообщением (выберите что
вы хотите сделать: *перечисление функций*)
10. if __name__ ...
пустой словарь "моя тел книга"
переменная выбор действия = запуск функции "получить ввод"

пока переменная выбор действия меньше 8 или равна 0
    если выбор действия = 1
        а вводится пользователем (Имя)
        б = значение функции "получить телефон"
        если не получилось вернуть б (получить телефон):
            напечатать "неправильный номер"
        сохранённый контакт - результат функции (арг: а,б,моя тел книга)
        если сохранённый контакт
            напечатать (сохранённый контакт, "сохранено")
        если выбор действия = 2
        а вводится пользователем (имя)
        номер телефона = функция  получить номер контакта (арг:
а, моя тел книга)
        если телефон:
            напечатать телефон
        иначе
            напечатать(а, "имя не надено")
        если выбор действия = 3
            б = функция получить телефона (надпись "телефон:")
            если не б:
                напечатать "неправильный телефон"
            имя = значение функции получить имя контакта (арг:
б, моя тел книга) 
            если есть имя:
                напечатать имя
            
            если выбор действия = 4
            а вводится пользователем ("имя: ")
            удалёный контакт = значение функции удалить контакт
            если есть удалёный контакт:
                напечатать (удалёный контак, "удалён")
            иначе:
                напечатать (а, "имя не надено")

        если выбор действия = 5
            а вводится пользователем ("имя: ")
            если получиться взять а из моей тел книги
                б = значение функции получить телефон("введите
новый телефон: ")
                если б:
                    новый контакт = значение функции (арг:а,б,моя тел
книга)
                    напечатать (новый контакт, "сохранено")
                иначе
                    напечатать "неправильный телефон
            иначе
                напечатать "имя не найдено"

            если выбор действия = 6:
                а = значение функции(моя тел книга)
                напечатать (а,"контактов имеет 3 или больше одинаковых 
цифр подряд)
            если выбор действия = 7
                напечатать все аргументы из моеё тел книги
            выбор действия = значение функции "получить ввод"